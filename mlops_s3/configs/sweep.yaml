program: src/docker_reproduce/train.py
name: sweepdemo
project: corrupt_mnist  # change this
#entity: ozkanyamaner  # change this
method: random
metric:
    goal: minimize
    name: validation_loss
parameters:
    lr:
        values: [0.01, 0.001, 0.0001]
    batch_size:
        values: [16, 32, 64]
    epochs:
        values: [5, 10]
run_cap: 10